[setup]
pkgs = ["nodejs-18_x", "python311", "gcc"]

[install]
cmds = ["npm install"]

[build]
cmds = ["npm run build", "pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
